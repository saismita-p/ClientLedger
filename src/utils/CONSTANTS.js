const CONSTANTS = {
  REGEX: {
    AlphaNumeric: /^[a-zA-Z\s]{2,30}$/,
    AlphaSpace: /^[a-zA-Z ]*$/,
    EmailRegex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    PhoneNumberRegex: /^\d{10}$/,
  },
  No_Data_Text: " No data available. Please refresh from the Dashboard.",
};

export default CONSTANTS;
